import MyAnimation from './Animation';

# Sorting


## Bubble Sort

```python
def bubble_sort(values):
    for i in range(0, N):
        for j in range(i + 1, N):
            if values[i] > values[j]:
                values[i], values[j] = values[j], values[i]
```


Here's an example of a different simple animation:
<MyAnimation />